package creational;

public class AbstractFactoryPattern {

    /*
    So lets say we add another dimension for Notification, lets say emergency and normal notification, in that
    case we can use Abstract Factory pattern.

    Abstract Factory is great for 2D variations,
    But once you add a 3rd or more, start moving to composition (Strategy, Builder, DI) over pure Abstract Factory.

    * */
}
